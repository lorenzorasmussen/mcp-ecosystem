#!/bin/bash
# Top-level access script for Desktop Control server
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec node "$DIR/categories/file-system/desktop-control/desktop_control_server.js" "$@"
