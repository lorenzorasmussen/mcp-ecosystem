apiVersion: v1
kind: Secret
metadata:
  name: mcp-client-bridge-secrets
  namespace: default
type: Opaque
data:
  # Base64 encoded values
  # JWT_SECRET: <base64_encoded_secret>
  # API_KEY: <base64_encoded_api_key>
  # DATABASE_URL: <base64_encoded_database_url>
  # MCP_SERVERS: <base64_encoded_json_array>